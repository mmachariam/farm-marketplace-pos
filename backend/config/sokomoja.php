<?php

return [

    // Password admins reset user accounts back to via
    // POST /api/admin/users/{id}/reset-password
    'default_user_password' => env('DEFAULT_USER_PASSWORD', 'SokoMoja2026!'),

];
